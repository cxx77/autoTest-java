server:
  port: 8888

logging:
  path: logs
  file: userManager.log

spring:
  application:
    name: userManager
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/study?characterEncoding=utf-8&useSSL=true
    username: root
    password: "000111"

mybatis:
  type-aliases-package: com.study.model
  mapper-locations:
    - mapper/*